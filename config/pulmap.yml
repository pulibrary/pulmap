defaults: &defaults
  events:
    server: 'amqp://localhost:5672'
    exchange: 'gbl_events'

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
  events:
    server: <%= ENV['PULMAP_RABBIT_SERVER'] || 'amqp://localhost:5672' %>
    exchange: 'gbl_events'

staging:
  <<: *defaults
  events:
    server: <%= ENV['PULMAP_RABBIT_SERVER'] || 'amqp://localhost:5672' %>
    exchange: 'gbl_events'
