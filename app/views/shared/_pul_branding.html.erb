<div class="l-branding__pul <%= layout_type %>-branding">
  <div class="<% unless index_layout? %>container<% end %> <%= layout_type %>-container ">
    <nav class="navbar fixed-top navbar-dark pul-header">
      <% if index_layout? %>
        <div class="logo__pul col-12 col-md-5 col-lg-4">
          <%= render :partial => 'shared/pul_logo_site_title' %>
        </div>
      <% else %>
        <div class="logo__pul col-12 col-md-6 col-lg-5">
          <%= render :partial => 'shared/pul_logo_site_title' %>
        </div>
      <% end %>

      <% if index_layout? %>
        <div class="col-12 col-md-5 col-lg-4">
          <%= render :partial=>'catalog/search_form_index' %>
        </div>
        <div class="d-none d-lg-block col-lg-1">
          <ul class="search-options">
            <li>
              <%= link_to 'Advanced', blacklight_advanced_search_engine.advanced_search_path(search_state.to_h), :class=>'advanced_search'%>
            </li>
          </ul>
        </div>
      <% else %>
        <div class="d-none d-md-block col-md-4">
        </div>
      <% end %>
      <div class="menu--level-1 d-none d-md-block col-md-2 col-lg-3">
        <ul>
          <li><a href="/about">About This Site</a></li>
          <li><a href="/contact-us">Contact Us</a></li>
          <%= render :partial=>'/account' %>
        </ul>
      </div>
    </nav>
  </div>
</div>
