<% if query_has_constraints? %>
      <div id="appliedParams" class="clearfix constraints-container">
        <%=link_to t('blacklight.search.start_over').html_safe, start_over_path, :class => "btn btn-primary button--start-over", :id=>"startOverLink" %>
        <div class="button--edit-search">
          <%=link_to t('blacklight.search.edit_search'), blacklight_advanced_search_engine.advanced_search_path(search_state.to_h), :class => "btn btn-default", :id=>"editSearchLink" %>
        </div>
        <div class="params">
          <span class="constraints-label sr-only"><%= t('blacklight.search.filters.title') %></span>
          <%= render_constraints(params) %>
        </div>
      </div>
<% end %>
