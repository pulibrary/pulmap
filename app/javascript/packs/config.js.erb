$(document).ready(() => {
  window.LEAFLET_GEOCODER_PROVIDER = '<%= Pulmap.config["leaflet_geocoder"]["provider"]["name"] %>';
  window.LEAFLET_GEOCODER_KEY = '<%= Pulmap.config["leaflet_geocoder"]["provider"]["key"] %>';
});
